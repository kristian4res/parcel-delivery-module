// Temporary types/interfaces until a unified ORDER schema is created
export type OrderRecord = {
    orderID: string;
    orderUUID: string;
    orderDetails: string;
}; 

export type OrderID = string;